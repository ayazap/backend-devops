apiVersion: v1
kind: Service
metadata:
  name: marriage-marriage-backend
  namespace: default
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: 3000
  selector:
    app: marriage-backend
