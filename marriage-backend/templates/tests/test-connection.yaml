apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "marriage-backend.fullname" . }}-test-connection"
  labels:
    {{- include "marriage-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "marriage-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
